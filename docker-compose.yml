services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.59.1-noble
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "9323:9323"
    command: sh -c "npm install && npx playwright test && npx playwright show-report --host 0.0.0.0"
